# QaafiyaDictBackEnd
Only the Programming Team has access to these files. In this the SQL file, the python Flask file and the html file are all attached. 
Before running the code be sure to import the code into your local SQL servers.
